.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
# HYDRA-UMC shared secret protection
.env
.env.*
!.env.example
# DEV-SERVER's own local workspace/state directories (13.5 of the
# software-prep plan) - real per-machine state, never committed. In
# production these live under /srv/hydra-umc-dev, outside any repo; kept
# here too in case local development ever creates them inside the checkout.
workspaces/
state/
artifacts/
logs/
cache/
